# Card

> Grouping related content into a bordered container with header and footer.

**Import**

```tsx
import { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent } from "@health-samurai/react-components";
```

**Props / states**

| Prop | Options | Default |
| --- | --- | --- |
| `variant` | `default` \| `round` \| `round-white` | — |

**Composition**

`Card` · `CardHeader` · `CardFooter` · `CardTitle` · `CardAction` · `CardDescription` · `CardContent`

**Links**

[Storybook](https://healthsamurai.github.io/aidbox-ts-sdk/react-components/?path=/docs/component-card--docs) · [Source](src/shadcn/components/ui/card.tsx) · [Story](src/shadcn/components/ui/card.stories.tsx)

_For the full prop list and a working example, `Read` the Source and Story files above._
