# ContextMenu

> Context actions triggered by right-click.

**Import**

```tsx
import { ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuItem, ContextMenuCheckboxItem, ContextMenuRadioItem, ContextMenuLabel, ContextMenuSeparator, ContextMenuShortcut, ContextMenuIcon, ContextMenuGroup, ContextMenuPortal, ContextMenuSub, ContextMenuSubContent, ContextMenuSubTrigger, ContextMenuRadioGroup } from "@health-samurai/react-components";
```

**Props / states**

| Prop | Options | Default |
| --- | --- | --- |
| `variant` | `default` \| `destructive` | `default` |

**Composition**

`ContextMenu` · `ContextMenuTrigger` · `ContextMenuContent` · `ContextMenuItem` · `ContextMenuCheckboxItem` · `ContextMenuRadioItem` · `ContextMenuLabel` · `ContextMenuSeparator` · `ContextMenuShortcut` · `ContextMenuIcon` · `ContextMenuGroup` · `ContextMenuPortal` · `ContextMenuSub` · `ContextMenuSubContent` · `ContextMenuSubTrigger` · `ContextMenuRadioGroup`

**Links**

[Storybook](https://healthsamurai.github.io/aidbox-ts-sdk/react-components/?path=/docs/component-context-menu--docs) · [Source](src/shadcn/components/ui/context-menu.tsx) · [Story](src/shadcn/components/ui/context-menu.stories.tsx)

_For the full prop list and a working example, `Read` the Source and Story files above._
