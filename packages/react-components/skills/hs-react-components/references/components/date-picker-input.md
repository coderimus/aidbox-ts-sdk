# DatePickerInput

> Date input field with a calendar popover.

**Import**

```tsx
import { DatePickerInput } from "@health-samurai/react-components";
```

**Type**

```ts
interface DatePickerInputProps {
	value: string;
	onChange: (value: string) => void;
	placeholder?: string;
	className?: string;
	disabled?: boolean;
}
```

**Links**

[Storybook](https://healthsamurai.github.io/aidbox-ts-sdk/react-components/?path=/docs/component-datepickerinput--docs) · [Source](src/components/date-picker-input.tsx) · [Story](src/components/date-picker-input.stories.tsx)

_For the full prop list and a working example, `Read` the Source and Story files above._
