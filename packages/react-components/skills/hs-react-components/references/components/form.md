# Form

> Wrapping form fields with validation and error state management.

**Import**

```tsx
import { useFormField, Form, FormItem, FormLabel, FormControl, FormDescription, FormMessage, FormField } from "@health-samurai/react-components";
```

**Composition**

`useFormField` · `Form` · `FormItem` · `FormLabel` · `FormControl` · `FormDescription` · `FormMessage` · `FormField`

**Links**

[Storybook](https://healthsamurai.github.io/aidbox-ts-sdk/react-components/?path=/docs/component-form--docs) · [Source](src/shadcn/components/ui/form.tsx) · [Story](src/shadcn/components/ui/form.stories.tsx)

_For the full prop list and a working example, `Read` the Source and Story files above._
