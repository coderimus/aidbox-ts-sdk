# Menubar

> Application menu bar with nested items, checkboxes, and shortcuts.

**Import**

```tsx
import { Menubar, MenubarPortal, MenubarMenu, MenubarTrigger, MenubarContent, MenubarGroup, MenubarSeparator, MenubarLabel, MenubarItem, MenubarShortcut, MenubarCheckboxItem, MenubarRadioGroup, MenubarRadioItem, MenubarSub, MenubarSubTrigger, MenubarSubContent } from "@health-samurai/react-components";
```

**Composition**

`Menubar` · `MenubarPortal` · `MenubarMenu` · `MenubarTrigger` · `MenubarContent` · `MenubarGroup` · `MenubarSeparator` · `MenubarLabel` · `MenubarItem` · `MenubarShortcut` · `MenubarCheckboxItem` · `MenubarRadioGroup` · `MenubarRadioItem` · `MenubarSub` · `MenubarSubTrigger` · `MenubarSubContent`

**Links**

[Storybook](https://healthsamurai.github.io/aidbox-ts-sdk/react-components/?path=/docs/component-menubar--docs) · [Source](src/shadcn/components/ui/menubar.tsx) · [Story](src/shadcn/components/ui/menubar.stories.tsx)

_For the full prop list and a working example, `Read` the Source and Story files above._
