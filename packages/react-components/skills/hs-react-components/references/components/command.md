# Command

> Searchable command palette (Cmd+K style).

**Import**

```tsx
import { Command, CommandDialog, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandShortcut, CommandSeparator } from "@health-samurai/react-components";
```

**Props / states**

| Prop | Options | Default |
| --- | --- | --- |
| `leftIcons` | `boolean` | `true` |
| `rightIcons` | `boolean` | `true` |
| `groups` | `boolean` | `true` |

**Composition**

`Command` · `CommandDialog` · `CommandInput` · `CommandList` · `CommandEmpty` · `CommandGroup` · `CommandItem` · `CommandShortcut` · `CommandSeparator`

**Links**

[Storybook](https://healthsamurai.github.io/aidbox-ts-sdk/react-components/?path=/docs/component-command--docs) · [Source](src/shadcn/components/ui/command.tsx) · [Story](src/shadcn/components/ui/command.stories.tsx)

_For the full prop list and a working example, `Read` the Source and Story files above._
